/* The one place the Amiga version number lives. The game shows it on the screen bar, and build/package_amiga.sh
 * names the package after it - the user tests by version number, so the two must never disagree. */
#ifndef BH_VERSION_H
#define BH_VERSION_H

#define BH_VERSION "v012"

#endif
